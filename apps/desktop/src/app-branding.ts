/**
 * Desktop shell human-visible product name (menu bar, About, packaged .app).
 * @module @deepseek-ai/dsh-desktop-shell/app-branding
 */

/** macOS menu bar / About dialog / electron-builder `productName`. */
export const DESKTOP_APP_DISPLAY_NAME = 'NanGeAGI'

/** About panel secondary line below the version. */
export const DESKTOP_APP_ABOUT_DETAIL = 'NanGeAGI desktop shell.'
