---
description: 本 fork 有定制改动时，同一轮工作内同步更新 CUSTOM.md
alwaysApply: true
---

# CUSTOM.md 变更记录

本仓库是 DeepSeek Harness 定制 fork。`CUSTOM.md` 是**相对官方的定制台账**，不是可选文档。

## 何时必须更新

在同一轮对话、同一 PR 或同一 commit 批次中，只要出现以下任一情况，**结束前**更新 `CUSTOM.md`（与代码/docs 同批提交，不要拖后补记）：

- 产品行为、UI 文案、默认配置相对 `custom/main` / upstream 有变化
- 新增或修改 `packages/`、`apps/web/`、`cordis.patch.yml` 等官方路径下的定制代码
- 新建/合并/废弃定制分支，或变更集成策略
- Host RPC、工具箱（details 栏）、文件编辑器等 V1 定制能力有增删改

## 写到哪里

按改动性质更新对应小节（可多条）：

| 小节 | 用途 |
|------|------|
| **已实现定制功能** | 用户可见能力、行为约定 |
| **我改过的官方文件** | 动过的路径与摘要 |
| **我的插件与组合包** | 新注册或职责变化 |
| **待合并 / 进行中** | 分支名、状态、关键 commit |
| **近期操作记录** | 按日期的操作流水（日期 / 操作 / 备注） |
| **我故意不跟的上游行为** | 与 upstream 的有意分歧 |

产品侧统一用 **工具箱** 表述右栏；代码符号仍可写 `details`。

## 不必记录

- 纯 upstream 合并、无本地语义变化
- 仅格式化、注释、与 fork 无关的机械修复
- 本地临时文件（`.sessions/`、`node_modules/`、未提交的 IDE 配置）

## 完成前自检

- [ ] `CUSTOM.md` 已反映本轮**全部**定制向改动
- [ ] 分支/commit 引用与 git 状态一致
- [ ] 未把仅计划未落地的内容写成「已实现」
