/** Human terminal plugin, node half. Browser behavior ships via exports["./client"]. */

/** Host plugin body — no host-side behavior for the human terminal plugin. */
export function apply(): void {}
