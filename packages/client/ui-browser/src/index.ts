/** Embedded browser plugin, node half. Browser behavior ships via exports["./client"]. */

/** Host plugin body — no host-side behavior for the embedded browser plugin. */
export function apply(): void {}
